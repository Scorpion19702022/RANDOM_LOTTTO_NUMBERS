const LottoNumbers = () => {
	return (
		<section>
			<div>
				<button>{`duży lotek`.toLocaleUpperCase()}</button>
				<button>{`duży lotek`.toLocaleUpperCase()}</button>
			</div>
		</section>
	)
}

export default LottoNumbers
